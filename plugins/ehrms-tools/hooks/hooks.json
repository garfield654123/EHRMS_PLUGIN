{
  "description": "ehrms-codegraph 記憶迴路保證：查完程式入口卻未沉澱記憶時，於回合結束前提醒補記（確定性 command hook，解析 transcript 的 tool_use 名稱判斷，不依賴模型自覺）",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "py \"${CLAUDE_PLUGIN_ROOT}/hooks/check_memory.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
